# PostgreSQL 연결 문자열 (Neon / Railway / Render 등에서 발급)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# 앱 접속 PIN (4자리). 미설정 시 기본값 0000
APP_PIN=5159

# 서버 포트 (PaaS는 보통 자동 주입하므로 비워둬도 됨)
PORT=5000
